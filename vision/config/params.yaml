vision_node:
  ros__parameters:
    bounding_box_topic: "/darknet_ros/bounding_boxes"         # the topic name associated with bounding boxes generated by darknet_ros
    service_name: "/dog_in_vision"                            # the exposed service name
    class_id: "dog"                                           # the class id used by darknet_ros for the class to check for